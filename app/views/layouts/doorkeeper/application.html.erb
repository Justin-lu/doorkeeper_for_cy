<!DOCTYPE html>
<html>
<head>
  <title>chance easy</title>
  <%= retrieve_assets("doorkeeper", "index") %>
  <%= csrf_meta_tags %>
</head>
<body>
  <section id="main" class="container">
    <div class="topbar">
      <div class="fill">
        <div class="container">
          <span class="brand">
            OAuth 服务端
          </span>
        </div>
      </div>
    </div>
    <div class="content">
      <div class="row">
        <% flash.each do |key, message| %>
          <div class="span16">
            <div class="alert-message <%= key %>" data-alert><a class="close" href="#">×</a><p><%= message %></p></div>
          </div>
        <% end %>

        <%= yield %>
      </div>
    </div>
  </section>
</body>
</html>
