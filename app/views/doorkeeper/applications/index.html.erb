<style>
input[type=submit] {
  margin-top: 15px;
}
</style>
<div class="span16">
  <header class="page-header">
    <h2>客户端列表</h2>
  </header>

  <p><%= link_to '添加一个客户端', new_oauth_application_path %></p>

  <table class="zebra-striped">
    <thead>
      <tr>
        <th>名字</th>
        <th>Callback路径</th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @applications.each do |application| %>
        <tr id="application_<%= application.id %>">
          <td><%= link_to application.name, [:oauth, application] %></td>
          <td><%= application.redirect_uri %></td>
          <td><%= link_to '编辑', edit_oauth_application_path(application) %></td>
          <td><%= render 'delete_form', application: application %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
